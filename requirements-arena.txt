# Arena layer (dashboard + LLM agents) -- additive on top of the JAX engine.
# Install into the same env you train in.

# --- M1: dashboard (data layer + graphs + renders) ---
streamlit>=1.36
plotly>=5.22
pandas>=2.0

# --- M2+: LLM agent crew (commentator / judges / coach / analyst) ---
# Uncomment when you reach M2. Hosted API only for now (local Ollama deferred).
# langchain>=0.2
# langchain-anthropic>=0.1
# langchain-openai>=0.1
# langgraph>=0.1
# crewai>=0.40
