# Lightweight deps to VIEW the dashboard on a shared snapshot — no JAX, no GPU.
# The arena/ dashboard reads runs/ with pandas + stdlib only, so a teammate can
# explore results / replays / narrations without the training stack:
#
#   pip install -r requirements-dashboard.txt
#   streamlit run arena/app.py
#
# Python 3.11 recommended (works on 3.10+). Versions match requirements.txt.

streamlit==1.58.0
plotly==6.7.0
pandas==3.0.2
numpy==2.4.4
pillow==12.2.0      # only needed if a run has a rendered .png/.gif rollout
imageio==2.37.3     # only needed for .mp4/.gif rollout playback
